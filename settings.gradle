pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        maven {
            url = uri("${rootDir}/local-maven-repo")
        }
        google()
        mavenCentral()
    }
}

rootProject.name = "android-cxx-thirdpartylib"
include ':lib'
include ':app'
